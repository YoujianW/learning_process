## 星宸板子编译烧录流程

### 文件树

```shell
root@YTF-NB-00090A:/work# tree -L 1
.
├── boot # u-boot
├── env_arm64.sh # 声明编译环境以及编译链
├── kernel # 内核部分
├── project # rootfs以及各种打包
├── sdk # 应用功能
└── toolchain # 编译链
```



### 编译

- 如果是第一次使用这个工程，未进行过任何编译，必须先进行全量编译，编译完成后生成的images在`project/image/output/images`

```shell
# 声明编译环境以及编译链
source /work/env_arm64.sh
# 进入project全量编译打包
cd /work/project
# 根据实际板子设置对应的deconfig
make dispcam_pcupid.emmc.glibc-10.2.1-squashfs.ssm001a.1024.dual_sensor.ab.bga_ddr4_riscv_defconfig
# 清除所有编译文件
make clean;
# 重新全量编译
make image -j8;
```

- 如果你已经全量编译了一次，这次只是修改了内核部分，仅编译kernel并打包出镜像即可

```shell
make linux-kernel_clean
make linux-kernel -j8
make image-fast-nocheck -j8 # 打包组装成镜像
```

- 如果你已经全量编译了一次，这次只是修改了u-boot部分，仅编译kernel并打包出镜像即可

```shell
make boot_clean
make boot -j8
make image-fast-nocheck -j8 # 打包组装成镜像
```

- Tips：编译过程中可能会有warning以及一个dtb的fatal，可以不用理会，只要最后结尾有 `kernel-image done!!!`、`PUSE check PASS!`、`codesize done`那就是编译通过了



###USB Device烧录

- 按正常流程编译整包sdk，生成image升级镜像文件后，需要将镜像文件转成bin文件，才能进行USB Device烧录
- make_usb_factory_sigmastar.sh  把 IPL + U-Boot + 各分区镜像 + 自动烧写脚本拼成**一个 USB 工厂烧录 bin**（给星宸 USB 烧录工具使用）

```shell
./image/makefiletools/script/make_usb_factory_sigmastar.sh \
  -i IPL.pcupid_usb.DDR4_3200_NTC8G_BGA2=y.bin \
  -u u-boot_emmc.xz.img.bin
```

- 可以选择全部升级和部分分区升级：

```shell
root@YTF-NB-00090A:/work/project# ./image/makefiletools/script/make_usb_factory_sigmastar.sh \
>   -i IPL.pcupid_usb.DDR4_3200_NTC8G_BGA2=y.bin \
>   -u u-boot_emmc.xz.img.bin
Full or Optional Upgrade ? (Y/N) # Y表示全部升级 N表示部分升级
```

```shell
Full or Optional Upgrade ? (Y/N)N
using alone TF-A:u-bl31.bin
Optional Upgrade [[set_partition.es? (Y/N)Y
Optional Upgrade [[boot.es? (Y/N)Y
Optional Upgrade [[riscvfw.es? (Y/N)Y
Optional Upgrade [[kernel.es? (Y/N)Y
Optional Upgrade [[rootfs.es? (Y/N)Y
Optional Upgrade [[misc.es? (Y/N)Y
Optional Upgrade [[miservice.es? (Y/N)Y
Optional Upgrade [[pstore.es? (Y/N)Y
Optional Upgrade [[vendor_storage.es? (Y/N)Y
Optional Upgrade [[customer.es? (Y/N)Y
Optional Upgrade set_config? (Y/N)Y
USB Facotry Image Generating.....
success, usb factory image have generated:
      path:./image/output/images/SstarUsbImage_202609160603.bin
      size:344629248 byte
      md5sum:2653e9764ed96c0a2872ae4
```

- 可以看到生成的bin文件为SstarUsbImage_202609160603.bin，打开星宸USB烧录工具就能进行烧录了，总结来说，不管你修改哪部分，都是要先make出image，然后转成bin包，烧录即可



### u-boot启动与烧录实际操作

- 首先找到一块串口小板，连接电脑与星宸板子的串口。

- 打开MobaXterm，选择串口，设置串口波特率为115200，打开串口。

- 接着用usb连接电脑与星宸板子的usb调试口，给板子上电，看到串口有数据后，快速按多次**Enter**，就不会进入linux，直接进入u-boot

- 输入以下命令直接进入device升级模式：

```shell
ufu command
```

- 输入命令后会停在如下位置，说明成功进入device升级模式

```shell
Hit any key to stop autoboot:
boot_key level=l
<USB> config miu select [70] [ef] [ef] [ef].
<USB>[UDC] PULL DOWN D+.
<USB>[UDC] PULL UP D+
<USB>[GADGET] UDC start
<USB>[UDC] PULL UP D+.
<USB>[UDC]PULL DOWN D+.
<USB>[UDC] PULL UP D+.
<USB>[LINK] High speed device.
<USB>[1][EN] bulk in with maxpacket/fifo(512/8192)
<USB>[2][EN] bulk out with maxpacket/fifo(512/1024)
```

- 打开星宸UsbFactoryTool 工具，可以观察到pc上有接入存储设备，选择好usb升级包文件，点击start按钮进行升级.